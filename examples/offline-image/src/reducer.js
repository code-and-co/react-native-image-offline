import { combineReducers } from 'redux';

import { offlineImageReducer } from 'react-native-image-offline';

const AppReducers = combineReducers({
    offlineImageReducer
});

export default AppReducers;
